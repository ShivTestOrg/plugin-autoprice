{
  "name": "plugin-autoprice",
  "description": "ts-template for UbiquityOS plugins.",
  "ubiquity:listeners": ["issues.opened", "issues.labeled", "issues.unlabeled", "issues.edited", "issue_comment.created"],
  "configuration": {
    "default": {},
    "type": "object",
    "properties": {
      "globalConfigUpdate": {
        "description": "Updates all price labels across all tasks based on `baseRateMultiplier` changes within the config file.",
        "type": "object",
        "properties": {
          "excludeRepos": {
            "examples": ["repo-name", "no-owner-required"],
            "description": "List of repositories to exclude from being updated",
            "type": "array",
            "items": {
              "type": "string"
            }
          }
        },
        "required": ["excludeRepos"]
      },
      "maxSimilarIssues": {
        "examples": [1, 5],
        "default": 5,
        "description": "Maximum number of similar issues for modifications.",
        "type": "integer"
      },
      "elevatedPriorityLabel": {
        "default": "Boosted",
        "description": "Label Name to identify boosted priority issues,",
        "type": "string"
      },
      "priorityMultiplier": {
        "examples": [1.5, 1.2, 1.1],
        "default": 1.1,
        "description": "Multiplier applied to prioritize similar issues.",
        "type": "integer"
      },
      "basePriceMultiplier": {
        "examples": [1.5],
        "default": 1,
        "description": "The base price multiplier for all tasks",
        "type": "number"
      }
    }
  }
}
